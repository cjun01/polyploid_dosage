from setuptools import setup, find_packages

setup(
    name="polyploid_dosage",
    version="0.0.1",
    packages=find_packages(),
    install_requires=[
        # Any dependencies your library has, for example:
        # 'numpy',
        # 'pandas',
        # ...
    ],
)
